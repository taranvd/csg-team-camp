import { FONT_SIZES } from './fonts.theme';
import { PADDINGS, MARGINS, MAX_WIDTH, WIDTH } from './sizes.theme';
import { SHADOWS } from './shadows.theme';
import { FONT_WEIGHTS } from './fontWeights.theme';
import { BORDER_RADIUS } from './borderRaduis.theme';

export const THEME = {
	fonst: {
		sizes: FONT_SIZES,
	},
	sizes: {
		paddings: PADDINGS,
		margins: MARGINS,
		maxWidth: MAX_WIDTH,
		width: WIDTH,
	},
	shadows: SHADOWS,
	fontWeights: FONT_WEIGHTS,
	borderRadius: BORDER_RADIUS,
};
